{
  "name": "tender-vault-functions",
  "description": "Tender Vault Cloud Functions (Charm Systems) — server-side automation, Claude parsing, matching, signed URLs.",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.3",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.1"
  },
  "devDependencies": {
    "eslint": "^8.57.1"
  }
}
